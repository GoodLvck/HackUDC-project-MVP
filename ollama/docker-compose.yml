services:
  ollama:
    image: ollama/ollama:0.17.4
    container_name: ollama
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    restart: unless-stopped
  
volumes:
  ollama_models:
    name: ollama_models